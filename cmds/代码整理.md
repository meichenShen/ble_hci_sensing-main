```text
请帮我重构当前项目中的 BLE CS 数据分析 notebook。

背景：
我现在有一个 notebook，文件名类似：
glb_load_cs_saved_frames_show_analysis.ipynb

这个 notebook 用于加载 BLE Host 应用保存的 CS 帧数据，并进行通道提取、缺失帧诊断、时间间隔分析、滤波、可视化等操作。目前大量代码都集中在 notebook 里，重复代码较多。我希望把稳定、通用、可复用的部分整理成 Python 模块，从而减少 notebook 里的代码量。

请按照以下要求进行重构。

---

# 一、总体目标

请不要重写整个项目，也不要改变现有的数据保存格式和核心分析逻辑。

目标是：

1. 保留 notebook 的主流程和解释性文字。
2. 将通用函数抽取到 `src/ble_analysis/` 目录下。
3. notebook 中只保留路径、参数、调用流程、少量任务相关逻辑和结果展示。
4. 尽量保证重构前后输出结果一致。
5. 不要删除原有 `data_saver.py` 和 `utils/signal_algrithom.py`，而是复用它们。

---

# 二、新增目录结构

请在 `src/` 下新增一个包：

```text
src/
  ble_analysis/
    __init__.py
    data.py
    channels.py
    diagnostics.py
    resampling.py
    filters.py
    plotting.py
```

如果项目中已有类似目录，请复用并补充，不要重复创建冲突目录。

---

# 三、模块职责

## 1. `data.py`

负责文件加载和基础数据读取。

请实现：

```python
def load_ble_frames(filepath, verbose=True):
    """
    Load BLE frame data using existing DataSaver.

    Parameters
    ----------
    filepath : str or Path
        Path to JSON or JSONL frame data.
    verbose : bool
        Whether to print loading information.

    Returns
    -------
    data : dict
        Loaded data object.
    frames : list
        data["frames"] if available, otherwise [].
    """
```

要求：

- 内部复用现有 `DataSaver`。
- 检查文件是否存在。
- 打印文件大小、加载状态、帧数量。
- 如果加载失败，返回 `(None, [])`。
- 不要修改 `DataSaver` 的原始实现。

---

## 2. `channels.py`

负责通道相关操作。

请实现：

```python
def get_available_channels(frames):
    """
    Return sorted list of all available channels in frames.
    """
```

```python
def find_channel_key(channels, channel):
    """
    Try to find a channel key in a channels dict.
    Support int and str channel keys.

    Returns the matched key, or None.
    """
```

```python
def extract_channel_series(frames, channel, verbose=True):
    """
    Extract amplitude, phase, local amplitude, remote amplitude and timestamps
    for a given channel from all frames.

    Returns a dict:
    {
        "channel": channel,
        "indices": np.ndarray,
        "timestamps_ms": np.ndarray,
        "time_sec": np.ndarray,
        "amplitudes": np.ndarray,
        "phases": np.ndarray,
        "local_amplitudes": np.ndarray,
        "remote_amplitudes": np.ndarray,
        "presence": list[bool],
        "missing_frames": list[dict],
    }
    """
```

要求：

- 通道号需要兼容整数和字符串 key。
- 不是所有帧都包含某个通道，这是正常情况，不要报错。
- `time_sec` 使用该通道第一个有效时间戳作为 0 点。
- 如果没有提取到数据，返回空数组并给出警告。
- 尽量保留当前 notebook 里的诊断字段：
  - frame_index
  - seq
  - timestamp
  - channels_in_frame

---

## 3. `diagnostics.py`

负责诊断和统计打印。

请实现：

```python
def print_file_info(data, frames):
    """
    Print file metadata and basic frame timing information.
    """
```

输出内容参考原 notebook，包括：

- 版本
- 保存时间
- 原始总帧数
- 保存的帧数
- 保存模式
- 第一帧 index 和 timestamp
- 最后一帧 index 和 timestamp
- 时间跨度
- 平均帧间隔
- 平均帧率

---

请实现：

```python
def diagnose_channel_presence(frames, channel, max_missing_to_print=10, verbose=True):
    """
    Diagnose whether a channel exists in each frame.

    Returns a dict:
    {
        "total_frames": int,
        "frames_with_channel": int,
        "frames_without_channel": int,
        "coverage": float,
        "presence": list[bool],
        "missing_frames": list[dict],
        "missing_gaps": list[int],
    }
    """
```

要求：

- 兼容 int 和 str channel key。
- 打印覆盖率。
- 打印前若干个缺失帧。
- 打印缺失帧间隔统计。

---

请实现：

```python
def analyze_time_intervals(timestamps_ms, plot=False, save_path=None, verbose=True):
    """
    Analyze timestamp intervals.

    Returns a dict:
    {
        "time_intervals_ms": np.ndarray,
        "time_intervals_sec": np.ndarray,
        "mean_interval": float,
        "std_interval": float,
        "min_interval": float,
        "max_interval": float,
        "cv": float,
        "estimated_sampling_rate": float,
        "large_gap_indices": np.ndarray,
        "n_large_gaps": int,
    }
    """
```

要求：

- 如果数据点不足，返回合理的空结果，不要报错。
- CV < 0.1 视为均匀。
- CV < 0.3 视为较均匀。
- CV >= 0.3 视为不均匀。
- 大间隔定义为 `time_interval > 2 * mean_interval`。
- `plot=True` 时绘制：
  - 时间间隔序列图
  - 时间间隔直方图
- 如果传入 `save_path`，保存图片。
- 函数返回实际采样率 `estimated_sampling_rate = 1.0 / mean_interval`。

---

## 4. `resampling.py`

负责插值、插帧、重采样。

请实现一个基础版本：

```python
def resample_to_uniform_grid(time_sec, values, target_fs=None, method="linear"):
    """
    Resample an irregular time series to a uniform time grid.

    Parameters
    ----------
    time_sec : array-like
        Original time axis in seconds.
    values : array-like
        Values to resample.
    target_fs : float or None
        Target sampling rate. If None, use 1 / mean(diff(time_sec)).
    method : str
        Currently support "linear".

    Returns
    -------
    result : dict
        {
            "time_sec": np.ndarray,
            "values": np.ndarray,
            "target_fs": float,
        }
    """
```

要求：

- 初始只支持 `"linear"` 即可。
- 使用 `np.interp`。
- 如果输入数据不足，返回空数组。
- 不要在 `load_ble_frames` 或 `extract_channel_series` 里自动重采样。
- 重采样必须由 notebook 显式调用。

---

## 5. `filters.py`

负责滤波函数包装。

项目中已有 `utils.signal_algrithom`，请优先复用它。

请实现：

```python
def apply_filter_pipeline(values, fs=None, pipeline=None):
    """
    Apply a sequence of filters to values.

    Parameters
    ----------
    values : array-like
    fs : float or None
    pipeline : list[dict] or str or None

    Returns
    -------
    filtered : np.ndarray
    """
```

基础要求：

- 如果 `pipeline is None`，直接返回原数组。
- 如果 `pipeline` 是 list，则按顺序执行。
- 支持以下 type，如果现有 `utils.signal_algrithom` 中存在对应函数就调用，否则先用简单实现或跳过并 warning：
  - `"median"`
  - `"hampel"`
  - `"lowpass"`
  - `"highpass"`
  - `"bandpass"`
- 对需要采样率的滤波器，如果 `fs is None`，给出 warning，并跳过该滤波器。
- 不要强行改变原 notebook 的滤波逻辑；如果不确定，优先保守处理。

示例 pipeline：

```python
pipeline = [
    {"type": "hampel", "window_size": 7, "n_sigmas": 3},
    {"type": "median", "kernel_size": 5},
    {"type": "highpass", "cutoff": 0.05, "order": 4},
]
```

---

## 6. `plotting.py`

负责绘图风格和通用绘图函数。

请实现：

```python
def setup_plot_style():
    """
    Set default matplotlib plotting style for BLE analysis.
    """
```

建议设置：

- figure size
- grid alpha
- axes title size
- label size
- line width
- font size
- savefig dpi
- savefig bbox

---

请实现：

```python
def plot_channel_amplitude_phase(series, channel=None, save_path=None, show=True):
    """
    Plot amplitude and phase over time for one channel.

    Parameters
    ----------
    series : dict
        Returned by extract_channel_series.
    channel : int or str or None
    save_path : str or Path or None
    show : bool

    Returns
    -------
    fig, axes
    """
```

要求：

- 上图：amplitudes over time。
- 下图：phases over time。
- x 轴为 `time_sec`。
- 如果没有数据，打印警告并返回 `(None, None)`。
- 如果 `save_path` 不为空，保存图片。
- `show=False` 时不要 `plt.show()`。

---

请实现：

```python
def plot_time_intervals(time_info, save_path=None, show=True):
    """
    Plot time interval sequence and histogram using result from analyze_time_intervals.
    """
```

---

# 四、更新 notebook

请更新当前 notebook，使其尽量使用新模块。

建议 notebook 顶部保留：

```python
import sys
from pathlib import Path

project_root = Path().resolve()
if (project_root / "src").exists():
    sys.path.insert(0, str(project_root / "src"))
else:
    project_root = project_root.parent
    sys.path.insert(0, str(project_root / "src"))

import numpy as np
import matplotlib.pyplot as plt

from ble_analysis.data import load_ble_frames
from ble_analysis.channels import extract_channel_series, get_available_channels
from ble_analysis.diagnostics import print_file_info, diagnose_channel_presence, analyze_time_intervals
from ble_analysis.plotting import setup_plot_style, plot_channel_amplitude_phase
from ble_analysis.resampling import resample_to_uniform_grid
from ble_analysis.filters import apply_filter_pipeline

setup_plot_style()
```

然后 notebook 主流程改成类似：

```python
filepath = "./sampleData/CS_frames_all_20260113_091339.jsonl"
channel = 2

data, frames = load_ble_frames(filepath)
print_file_info(data, frames)

available_channels = get_available_channels(frames)
print(f"可用通道数: {len(available_channels)}")
print(f"前20个通道: {available_channels[:20]}")

presence_info = diagnose_channel_presence(frames, channel)

series = extract_channel_series(frames, channel)

time_info = analyze_time_intervals(
    series["timestamps_ms"],
    plot=True,
    save_path="./outputs/channel_2_time_intervals.png",
)

actual_sampling_rate = time_info.get("estimated_sampling_rate", None)

plot_channel_amplitude_phase(
    series,
    channel=channel,
    save_path="./outputs/channel_2_amp_phase.png",
)
```

如果原 notebook 后面还有滤波或任务相关处理，暂时不要强行重构太多，只需要把明显重复、通用的部分替换成模块调用。

---

# 五、保留行为和兼容性要求

1. 不要删除原 notebook 的重要说明文字。
2. 不要改变数据文件格式。
3. 不要改变 `DataSaver` 的接口。
4. 不要强制重采样。
5. 不要在加载时偷偷插值或补帧。
6. 通道缺失是正常现象，不要 raise exception。
7. 对 int 和 str 类型的 channel key 都要兼容。
8. 所有函数尽量支持 `verbose=True/False`。
9. 如果某些滤波函数找不到，给出 warning，而不是让程序崩溃。
10. 生成图片时确保 `outputs/` 目录存在。

---

# 六、期望效果

重构后，notebook 中不再出现大段重复的：

- 通道存在性检查代码；
- missing_frames 构造代码；
- 时间间隔统计代码；
- 时间间隔绘图代码；
- 幅值相位绘图代码；
- 文件元信息打印代码。

这些都应该转移到 `src/ble_analysis/` 中。

notebook 应该主要表达实验流程，而不是堆大量工具代码。

---

# 七、请先做小步重构

请优先完成：

1. `data.py`
2. `channels.py`
3. `diagnostics.py`
4. `plotting.py`

然后再处理：

5. `resampling.py`
6. `filters.py`

如果原 notebook 太长，请不要一次性重写全部内容。可以先重构前半部分，也就是：

- 导入
- 加载文件
- 查看文件信息
- 提取通道数据
- 数据点数量诊断
- 时间戳间隔分析
- 基础幅值/相位绘图

确保这些能运行后，再继续重构后面的滤波和任务相关代码。
```

---

## 你也可以再补一句给 Cursor

如果你希望它更稳一点，可以在最后补充：

```text
请在修改前先阅读 notebook 和现有 src/ 目录结构。尽量保持原 notebook 的输出含义不变。请不要为了“美观”重写业务逻辑；这次重构的重点是抽取公共函数和减少重复代码。
```

---



## 额外注意

请把 notebook 中直接导入 `utils.signal_algrithom` 的逻辑移到 `src/ble_analysis/filters.py` 中。

要求：

1. notebook 不再直接执行：
   `from utils import signal_algrithom as sig_algm`

2. notebook 只通过以下方式使用滤波功能：
   `from ble_analysis.filters import apply_filter_pipeline`

3. 在 `src/ble_analysis/filters.py` 内部使用 try/except 导入：
   `from utils import signal_algrithom as sig_algm`

4. 如果导入失败，`sig_algm = None`，并在实际调用相关滤波器时 warning，而不是报错。

5. 不要移动或改名 `src/utils/signal_algrithom.py`。

6. 输出目录不要再使用相对于 notebook 当前工作目录的 `Path("./outputs")`，而是使用项目根目录下的：
   - `outputs/figures/`
   - `outputs/processed/`
   - `outputs/reports/`

7. 如果方便，请新增 `src/ble_analysis/paths.py`，包含：
   - `find_project_root(start=None, marker="src")`
   - `ensure_output_dirs(project_root=None)`

8. notebook 顶部先找到 `project_root` 和 `src_dir`，把 `src_dir` 加入 `sys.path`，然后导入 `ble_analysis` 里的函数。

9. 保持 notebook 原有分析流程不变，只替换导入方式和输出目录创建方式。